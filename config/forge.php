<?php

declare(strict_types=1);

/**
 * 默认配置（route-forge/laravel）
 *
 * 发布到宿主项目后通过 config/forge.php 修改。
 *
 * @see .docs/SPEC.md §3.1.2, §5
 */
return [

    /*
    |--------------------------------------------------------------------------
    | 层级定义表
    |--------------------------------------------------------------------------
    |
    | 键为层级名（完全可自定义，包不预设固定层级）。详见 DESIGN.md §6.2。
    |
    | 每个层级的可用字段（对应 SPEC §5）：
    |
    |   description            string   ''    层级描述，仅用于文档与调试输出
    |   load                   'eager'|'lazy'
    |                              'eager'    是否在摘要端点中标记为「前端应预加载」
    |                              'lazy'     前端自动发现时据此决定预加载策略
    |   match.prefix           string[] []    URI 前缀匹配列表，命中任一即归入此层级；
    |                          |string        也接受单个字符串（等价于只含它的数组，需 common ≥ 1.1.1），
    |                                         空字符串仍按「空前缀」跳过、不会命中所有路由
    |   match.middleware       string[] []    中间件匹配列表，匹配逻辑受 middleware_match 控制；
    |                          |string        同样接受单个字符串（需 common ≥ 1.1.1）
    |   match.middleware_match string  'any'  中间件匹配模式：'any'（OR）/ 'all'（AND）/
    |                          array          DNF 数组（见 §3.1.2）；只接受 string 或 array，
    |                                         其他类型回落 'any' 并记录 warning
    |   endpoint_middleware    string[]       访问该层级元信息端点时要求的中间件列表，未配置则不限制；
    |                          |string|null   与 Laravel 的 ->middleware() 同形，接受单个字符串
    |
    */
    'levels'            => [
        'public' => [
            'description' => '公共接口（无需登录）',
            'match' => [
                'prefix'     => ['auth', 'public'],
                'middleware' => [],
            ],
            'load'  => 'eager',
        ],
        'client' => [
            'description' => '客户端用户接口',
            'match' => [
                'prefix'     => ['client'],
                'middleware' => ['auth'],
            ],
            'load'  => 'lazy',
        ],
        'manage' => [
            'description' => '运营管理接口',
            'match' => [
                'prefix'     => ['manage'],
                'middleware' => ['auth', 'manage'],
            ],
            'load'  => 'lazy',
        ],
        'admin'  => [
            'description' => '系统管理接口',
            'match' => [
                'prefix'     => ['admin'],
                'middleware' => ['auth', 'admin'],
                'middleware_match' => 'all',
            ],
            'load'  => 'lazy',
            // 'endpoint_middleware' => ['auth', 'admin'],  // 访问 /_forge/routes/admin 需要 auth + admin
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 路由元信息端点前缀
    |--------------------------------------------------------------------------
    |
    | 同时用于层级端点（GET /{prefix}/{level}）和摘要端点（GET /{prefix}）。
    | 可通过 FORGE_ENDPOINT_PREFIX 环境变量覆盖。
    |
    */
    'endpoint_prefix'   => env('FORGE_ENDPOINT_PREFIX', '/_forge/routes'),

    /*
    |--------------------------------------------------------------------------
    | 路由前缀（url_prefix）
    |--------------------------------------------------------------------------
    |
    | 应用的路由前缀，通过摘要端点 config.url_prefix 下发给前端。
    | 支持两种格式：
    |   - 完整 URL（含协议和域名）：'https://api.example.com/v1'
    |   - 仅路径前缀：'/api/v1'
    |
    | null 或空字符串 = 不下发（前端视为无前缀）。
    |
    */
    'url_prefix' => env('FORGE_URL_PREFIX'),

    /*
    |--------------------------------------------------------------------------
    | 摘要端点中间件
    |--------------------------------------------------------------------------
    |
    | 访问摘要端点（GET /{endpoint_prefix}）时要求的中间件列表。
    | null 或空数组 = 不限制访问。
    |
    */
    'endpoint_middleware' => [],

    /*
    |--------------------------------------------------------------------------
    | 缓存 TTL（秒）
    |--------------------------------------------------------------------------
    |
    | 统一控制所有层级端点与摘要端点的缓存 TTL。
    | null = 不缓存（每次实时扫描路由表）
    | 0 = 永久缓存（Laravel Cache 惯例，
    | ⚠ 非 HTTP Cache-Control: max-age=0 含义）。
    | 负值 = 视为 null（不缓存）。
    |
    */
    'cache_ttl'         => env('FORGE_CACHE_TTL', 3600),

    /*
    |--------------------------------------------------------------------------
    | 缓存驱动
    |--------------------------------------------------------------------------
    |
    | null = 使用默认缓存驱动；可指定 redis / file / array 等。
    |
    */
    'cache_driver'      => env('FORGE_CACHE_DRIVER'),

    /*
    |--------------------------------------------------------------------------
    | 严格模式
    |--------------------------------------------------------------------------
    |
    | true  = 未命中层级的路由抛 RouteTierNotAssignedException。
    | false = 未命中路由归入 unassigned 特殊层级，
    |         经 GET /{endpoint_prefix}/unassigned 获取（见 SPEC §3.1.6）。
    |
    */
    'strict_mode'       => env('FORGE_STRICT_MODE', false),

    /*
    |--------------------------------------------------------------------------
    | 摘要端点响应格式版本（schemeVersion）
    |--------------------------------------------------------------------------
    |
    | 摘要端点（GET /{endpoint_prefix}）返回的响应格式版本号，
    | 前端据此识别格式并做版本兼容。默认 1，后续迭代引入不兼容的
    | 格式变更时递增。
    |
    */
    'scheme_version'    => env('FORGE_SCHEME_VERSION', 1),

    /*
    |--------------------------------------------------------------------------
    | 路由别名映射表（aliases）
    |--------------------------------------------------------------------------
    |
    | 键 = 别名（旧路由名，前端继续使用），值 = 真实路由名（新名）。
    | 用于路由改名迭代：前端调用方无需修改路由名即可继续工作。
    |
    | 别名条目由 RouteRepository 注入目标路由所在层级的元信息
    | （GET /{prefix}/{level} 的 routes 对象中同时出现新旧两个名字，
    | 指向完全相同的元信息），route:forge:types 亦为别名生成类型条目。
    |
    | 声明通道与优先级（SPEC §3.1.7）：
    |   - 路由宏 ->forgeAlias('旧名')（显式，优先级高于本配置）
    |   - 本配置表（适合批量迁移与集中管理；长期稳定对外名也建议集中在此维护）
    |   - 别名与真实路由名撞车 → 真实路由优先，别名被忽略（list/管理器给出警告）
    |   - 别名指向不存在的路由名 → 抛 AliasTargetException（RF_BE_008，fail-fast）
    |
    | 两种用法（详见 SPEC §3.1.7）：
    |   1. 改名迁移过渡：改名后声明旧名，前端零改动，稳定后清理；
    |   2. 长期稳定对外名：给易变路由固定一个对外调用名，此后路由改名/调整层级
    |      只需更新本映射的目标值，前端永不感知（约定前端只调用别名）。
    |
    | 示例：
    |   'aliases' => [
    |       'admin.users.index' => 'admin.members.index',
    |   ],
    |
    */
    'aliases'           => [],

    /*
    |--------------------------------------------------------------------------
    | 自定义分类回调
    |--------------------------------------------------------------------------
    |
    | 签名 fn(Route $r): ?string，返回层级名或 null。
    | 优先级介于「显式 tier / group tier」与「配置 match」之间（见 SPEC §3.1.4）。
    | 返回的层级名必须在 levels 配置中存在，否则抛 UnknownClassifierTierException。
    |
    | 示例：按 Controller 命名空间归类
    |   'classifier' => fn(\Illuminate\Routing\Route $r): ?string => str_contains($r->getAction()['controller'] ?? '', 'Admin\\') ? 'admin' : null,
    |
    */
    'classifier'        => null,

    /*
    |--------------------------------------------------------------------------
    | 管理器页面允许访问的 IP 列表（manager_allowed_ips）
    |--------------------------------------------------------------------------
    |
    | 仅对管理器页面路由（/_forge/manager*）生效的 IP 白名单。
    |
    | ⚠ 本配置只在开发环境有意义：管理器路由仅在 APP_DEBUG=true 时注册，
    |   线上（APP_DEBUG=false）根本不注册管理器路由，可无视本配置。
    |
    | 取值语义：
    |   - IP 列表（精确匹配）：仅列表中的来源 IP 可访问，其余 403；
    |     默认 ['127.0.0.1', '::1'] 仅允许本机访问
    |     （浏览器访问 localhost 可能解析为 IPv6 的 ::1，故一并放行）；
    |   - 列表中包含 '*'：放行任意来源；
    |   - 单个字符串（'192.168.1.10' 或 '*'）：等价于只含该值的数组，
    |     与 Laravel ->middleware('auth') 同形；
    |   - null 或空数组：不做 IP 限制（显式放开，局域网环境注意暴露风险）；
    |   - 本键完全缺失：按上面的默认值处理（仅本机），不会当作「不限制」。
    |
    | 局域网调试：把开发机的局域网 IP 追加进列表即可，例如
    |   'manager_allowed_ips' => ['127.0.0.1', '::1', '192.168.1.10'],
    |
    */
    'manager_allowed_ips' => ['127.0.0.1', '::1'],
];
